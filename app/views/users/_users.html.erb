<% users.each do |user| %>
  <div>
    <%# <%= image_tag user.avatar.variant(resize_to_limit: [50, 50]) if user.avatar.attached? %>
    <%= image_tag user.avatar.url, width: "50" if @user.avatar.file %>
    <%= link_to  user.email, user %>
    <%= link_to  "BTN", "#" %>
  </div>
<% end %>
